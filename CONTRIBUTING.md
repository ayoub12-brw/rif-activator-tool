# المساهمة في RiF Activator A12+ 🤝

نرحب بمساهماتكم في تطوير وتحسين RiF Activator A12+! هذا الدليل سيساعدك على فهم كيفية المساهمة بفعالية.

## 📋 جدول المحتويات

- [قواعد السلوك](#قواعد-السلوك)
- [كيفية المساهمة](#كيفية-المساهمة)
- [الإبلاغ عن الأخطاء](#الإبلاغ-عن-الأخطاء)
- [طلب ميزات جديدة](#طلب-ميزات-جديدة)
- [إرشادات الكود](#إرشادات-الكود)
- [عملية المراجعة](#عملية-المراجعة)

## 🤝 قواعد السلوك

- كن محترماً ومهذباً مع جميع المساهمين
- استخدم لغة بناءة وإيجابية
- ركز على ما هو الأفضل للمجتمع والمشروع
- تقبل النقد البناء بصدر رحب

## 🚀 كيفية المساهمة

### 1. Fork المشروع
```bash
# انقر على Fork في الصفحة الرئيسية للمشروع
# ثم استنسخ النسخة الخاصة بك
git clone https://github.com/yourusername/rif-activator-a12plus.git
cd rif-activator-a12plus
```

### 2. إعداد البيئة المحلية
```bash
# إنشاء بيئة افتراضية
python -m venv venv
source venv/bin/activate  # Linux/Mac
# أو
venv\Scripts\activate     # Windows

# تثبيت المتطلبات
pip install -r requirements.txt
pip install -r requirements-dev.txt  # أدوات التطوير
```

### 3. إنشاء Branch جديد
```bash
git checkout -b feature/your-feature-name
# أو
git checkout -b bugfix/your-bugfix-name
```

### 4. إجراء التغييرات
- اكتب كود نظيف ومفهوم
- أضف تعليقات باللغة العربية
- اتبع إرشادات الكود أدناه

### 5. اختبار التغييرات
```bash
# تشغيل الاختبارات
python -m pytest tests/

# اختبار الواجهة
python app_simple.py
```

### 6. Commit التغييرات
```bash
git add .
git commit -m "feat: إضافة ميزة جديدة"
# أو
git commit -m "fix: إصلاح مشكلة في التحقق"
```

### 7. Push و Pull Request
```bash
git push origin feature/your-feature-name
```
ثم افتح Pull Request في GitHub

## 🐛 الإبلاغ عن الأخطاء

عند الإبلاغ عن خطأ، يرجى تضمين:

### معلومات النظام
- نظام التشغيل وإصداره
- إصدار Python
- إصدار المشروع

### وصف المشكلة
- خطوات إعادة إنتاج المشكلة
- السلوك المتوقع vs السلوك الفعلي
- رسائل الخطأ كاملة
- لقطات شاشة إن أمكن

### مثال على تقرير خطأ جيد
```markdown
**البيئة:**
- Windows 11
- Python 3.10
- RiF Activator v2.0.0

**وصف المشكلة:**
عند محاولة فحص جهاز iPhone XS، يظهر خطأ 500

**خطوات إعادة الإنتاج:**
1. افتح الواجهة على المتصفح
2. اذهب إلى صفحة فحص الجهاز
3. أدخل: iPhone11,2
4. اضغط "فحص"

**رسالة الخطأ:**
```
Internal Server Error
```

**السلوك المتوقع:**
عرض معلومات دعم الجهاز
```

## ✨ طلب ميزات جديدة

عند طلب ميزة جديدة:

1. **ابحث أولاً**: تأكد أن الميزة غير موجودة
2. **اشرح الحاجة**: لماذا هذه الميزة مهمة؟
3. **قدم أمثلة**: كيف ستعمل الميزة؟
4. **ناقش التنفيذ**: اقتراحات للتطبيق

## 📝 إرشادات الكود

### Python Code Style
```python
# استخدم PEP 8
# أسماء المتغيرات بالإنجليزية
# التعليقات بالعربية

def check_device_support(device_model):
    """
    فحص دعم الجهاز في قاعدة البيانات
    
    Args:
        device_model (str): موديل الجهاز مثل iPhone11,2
        
    Returns:
        dict: معلومات دعم الجهاز
    """
    # التحقق من صحة المدخلات
    if not device_model:
        return {'error': 'موديل الجهاز مطلوب'}
    
    # البحث في قاعدة البيانات
    result = database.query(device_model)
    return result
```

### Frontend Code Style
```javascript
// استخدم camelCase
// التعليقات بالعربية
// كود نظيف ومقروء

async function checkDevice() {
    // جلب معلومات الجهاز من النموذج
    const deviceModel = document.getElementById('deviceModel').value;
    
    try {
        // إرسال طلب للخادم
        const response = await fetch('/api/check_device', {
            method: 'POST',
            headers: {
                'Content-Type': 'application/json'
            },
            body: JSON.stringify({ device_model: deviceModel })
        });
        
        const result = await response.json();
        displayResult(result);
    } catch (error) {
        console.error('خطأ في فحص الجهاز:', error);
    }
}
```

### CSS Style Guide
```css
/* استخدم BEM methodology */
/* متغيرات CSS للألوان */

:root {
    --primary-color: #667eea;
    --secondary-color: #764ba2;
    --success-color: #4CAF50;
    --error-color: #f44336;
}

.device-card {
    /* الخصائص مرتبة أبجدياً */
    background: var(--primary-color);
    border-radius: 15px;
    padding: 20px;
    transition: transform 0.3s ease;
}

.device-card:hover {
    transform: translateY(-5px);
}
```

## 🔍 عملية المراجعة

### معايير القبول
- ✅ الكود يعمل بدون أخطاء
- ✅ اتباع إرشادات الكود
- ✅ إضافة اختبارات للميزات الجديدة
- ✅ توثيق واضح للتغييرات
- ✅ عدم كسر الوظائف الموجودة

### ما يُراجع
1. **الوظائف**: هل تعمل كما هو متوقع؟
2. **الأداء**: هل هناك تحسينات ممكنة؟
3. **الأمان**: هل هناك ثغرات محتملة؟
4. **إمكانية الصيانة**: هل الكود سهل الفهم؟

## 🏷️ تسمية Commits

استخدم الأنماط التالية:
- `feat: إضافة ميزة جديدة`
- `fix: إصلاح خطأ`
- `docs: تحديث الوثائق`
- `style: تحسينات التنسيق`
- `refactor: إعادة هيكلة الكود`
- `test: إضافة اختبارات`
- `chore: مهام صيانة`

## 🎯 أولويات التطوير

### عالية الأولوية
- إصلاح الأخطاء الأمنية
- تحسين الأداء
- دعم أجهزة جديدة

### متوسطة الأولوية
- ميزات جديدة
- تحسين واجهة المستخدم
- توثيق أفضل

### منخفضة الأولوية
- تحسينات تجميلية
- تحسين الكود
- أدوات تطوير

## 🆘 الحصول على المساعدة

- 💬 **Discussions**: للأسئلة العامة
- 🐛 **Issues**: للأخطاء والميزات
- 📧 **البريد الإلكتروني**: للاستفسارات المباشرة

## 🙏 شكر وتقدير

شكراً لك على اهتمامك بالمساهمة! كل مساهمة، مهما كانت صغيرة، تساعد في تحسين المشروع للجميع.

---

**تذكر**: الهدف هو بناء أداة رائعة ومفيدة للمجتمع العربي. مساهمتك تصنع الفرق! 🚀